# Open Questions

Blocker means Claude Code should stop and ask before building the affected part. Non-blocker means build with the stated default and note it.

| ID | question | default if unanswered | blocker |
|---|---|---|---|
| OQ-1 | Exact turnover folder structure from shooters and desired delivery folder layout **2026-09-19, one sample**: `Turnover199/` is flat: five camera MP4s, a `.drt` and a metadata CSV, no side files (`docs/SAMPLE_TURNOVER_199.md`) | Recursive search for media; layout in NAMING_SPEC section 5 | no |
| OQ-2 | ~~Shot tracker column layout~~ **ANSWERED 2026-09-11**: the user supplied the real sheet, `docs/Pre Pro Shot Tracker - W1.csv`, 790 rows across 55 turnovers. **39 columns, of which the tool owns nine**; the rest is production state the vendor's team fills in for weeks after delivery, so the export is additive rows in the tracker's own column order and never a sheet of ours. The full mapping is in QC_RULES under the QC log structure. Worth recording what the guess cost: the default template described there before shared exactly **one** column with the real sheet, and the four it invented that the tracker has no room for (duration, source TC in and out, version, delivery path) stay in the QC log instead. Two further things fell out of the same file and are their own rows: OQ-19 reopened, and OQ-41 | n/a, answered | no |
| OQ-3 | ~~What codec/container is the consolidated media~~ **RESOLVED 2026-09-11, then narrowed 2026-09-11.** The container is settled and unchanged: **ProRes 4444**, DNxHR 444 where ProRes is unavailable, 12 bit 4:4:4 full range, and QC-020 and QC-021 stand. What is no longer settled is the **encoding**. The 2026-09-11 answer was ACEScct from a studio template project, which made a turnover camera agnostic; the final colour workflow has the shooters deliver **camera original log** instead, so the encoding is per shot. **Reversed on 2026-09-11 to one studio standard, and reversed back on 2026-09-12**, which is three positions in two days and the reason nothing about the source encoding should be taken from memory. Current: camera native log, named in the **clip's metadata** rather than in a sidecar (OQ-44), with a studio standard available as a Settings mode (OQ-39). The camera agnostic property is gone again and OQ-34 is the bill for it | container as specified in COLOR_AND_FORMAT section 2; encoding per shot from the clip metadata | no |
| OQ-4 | How shooters name reference stills and BTS on the timeline (as clips named `MELT0001_pl01_colorChart_01`?) or are they side files? **2026-09-19, the sample**: three single-frame clips sit on the timeline (`Duration` 1, a second of handle either side in the file), which is the timeline-clip half of the default; they are named as camera clips, not `..._colorChart_01`, so nothing says which still is which (OQ-62) **2026-09-19, later: the stills are timeline clips with camera names, and their identity is metadata.** The sample's three stills carry `Shot` = `TEST0001` like the plates, so which is the colour chart and which the grey ball is not in any field yet: OQ-70 | Support both: timeline aux clips and side files matched by name | no |
| OQ-5 | Spec typos: 2161/2162 heights, witness cam listed without frame range. **Witness cam half CONFIRMED by the user 2026-09-10: `wit` is a normal deliverable and is treated exactly like any other clip on the timeline**, so it gets the same four picture deliverables as `cp`, `el` and `re` (raw 4k, raw HD, ref 4k, ref HD) and no audio, and its raw output is a sequence. The missing frame range in the PDF is a defect in the table, not a statement that witness cam is delivered differently. Do not re-derive this from the PDF: it reads as though it means something and it does not. The 2161/2162 heights remain real typos, treated as 3840x2160 | as built; `tests/test_planner.py::TestTypeTable` pins it | no |
| OQ-6 | ~~Display transform for ref mp4s and stringout~~ **SUPERSEDED 2026-09-11 by the ACES pipeline.** The question assumed the tool would pick a transfer curve. It no longer does: the display encode is the ACES output transform for sRGB, supplied by OpenColorIO, and a bare transfer curve is not a thing the tool applies any more. What remains of the question moved to OQ-29, which is about **which** ACES output transform | ACES output transform for sRGB, from the built-in config | no |
| OQ-7 | ~~Downscale for EXR-source path~~ **RESOLVED 2026-09-10**: neither. `core/resize.py` implements antialiased Lanczos-3 in numpy, the same filter `scale=1920:1080:flags=lanczos` applies on the container path, so the two paths agree: on a hard edge the outputs are identical, and on smooth content they differ by under one 8 bit level. `scipy.ndimage.zoom` was rejected because a cubic spline does not antialias (a 2:1 reduction samples the source instead of integrating it) and it would add a large dependency to a 300 MB installer budget | numpy Lanczos-3, no new dependency | no |
| OQ-8 | ~~ffmpeg licensing and source~~ **RE-RESOLVED 2026-09-10 for the macOS target**: gyan.dev publishes Windows builds only (419 assets across 100 releases checked, zero macOS), so the Windows pin is gone. v01 bundles the **martin-riedl.de macOS arm64 GPL v3 build, ffmpeg 9.0.1**, which carries libx264, libzimg for `zscale`, libfreetype for `drawtext` and `h264_videotoolbox`. The licensing reasoning is unchanged and still holds: GPL v3 applies to the binaries alone, ProIngest calls them as subprocesses and is not linked against them, and the tool stays inside the studio so no source offer is triggered. Settings override retained. Rationale, rejected alternatives and the verification method are in `proingest/resources/ffmpeg/PROVENANCE.md` | martin-riedl.de macOS arm64 GPL build, x264 with CRF as specified | no |
| OQ-9 | **Apple Developer ID for signing and notarization?** Confirmed 2026-09-10 that there is no account today. This is materially worse on macOS than the Windows SmartScreen case it replaces: a downloaded, quarantined, unsigned and un-notarized `.app` is **blocked outright by Gatekeeper**, not warned about. The bundled ffmpeg binaries are already Developer ID signed with the hardened runtime, so they are not the problem; ProIngest's own bundle is. Workarounds in PACKAGING.md: buy a Developer ID ($99/yr), or transfer the app by a route that never sets `com.apple.quarantine` (scp, rsync, USB), or run `xattr -dr com.apple.quarantine` once on the target machine | Unsigned, handed over without quarantine, xattr stripped on install | **yes, before handover** |
| OQ-10 | Should a shot with only a `cp` and no `pl` be allowed? | Allowed, info only | no |
| OQ-11 | **Camera data file format: free text or key: value lines? Which keys go to the tracker?** **Built to the default 2026-09-11** (M4.3, `core/camdata.py`, QC-053): every `key: value` line is a pair and all of them go to the QC log's Camera Data sheet. Two things the build learned. The files arrive as **RTF** as often as plain text - 34 of the studio tracker's camData entries are `.rtf` - so the reader strips RTF first, pragmatically rather than properly. And a free text line containing a colon becomes a pair: `Shot at 14:32` reads as `Shot at 14` = `32`. That noise is accepted rather than filtered, because filtering means a list of keys that count as real, which is exactly the kind of table that looks right and is not. **The tracker half of the question is answered and the answer is none**: the real sheet (OQ-2) has no column for a camera field, so nothing from here reaches it  **CLOSED 2026-09-22, unanswered and no longer needed.** camData carries no `Shot Type`, so the tool ignores it and does not deliver it (user). `core/camdata.py`, QC-053 and the QC log's Camera Data sheet come out; the RTF finding is kept in this entry because it is true and would matter again if camData ever came back.| Parse `key: value` lines, dump all to the Camera Data sheet | no |
| OQ-12 | ~~Stringout audio: include, or video only?~~ **CLOSED 2026-09-11: the tool does not build a stringout.** The colour session exports one (PRD FR-9) | n/a, removed | no |
| OQ-13 | EXR half vs full float | half | no |
| OQ-14 | Multiple `.otio` files in one turnover folder | Prompt user to pick | no |
| OQ-15 | ~~Shooter name normalization in the stringout filename~~ **CLOSED 2026-09-11 with the stringout.** The answer was built and is still in `naming.normalize_shooter`, which is now unreachable from any planned deliverable; the unnormalized shooter name is still wanted on the turnover for the tracker and the QC log, so the field stays | Lowercase and strip everything outside `a-z0-9`; the code stays, unused, until someone decides to remove it | no |
| OQ-16 | ~~`ffprobe.exe` missing~~ **SUPERSEDED 2026-09-10 by the macOS target**: the Windows binaries are no longer bundled. martin-riedl.de publishes ffmpeg and ffprobe as separate downloads from one build id, so both are pinned individually in `build/ffmpeg.lock.json` and report the same version string. Question returns with the v02 Windows build | both binaries present and version-matched | no |
| OQ-17 | ~~Colour space conflict~~ ~~RESOLVED 2026-09-10~~ **SUPERSEDED 2026-09-11, and the earlier resolution was wrong.** It recorded that turnovers carry a baked sRGB curve on every file, that sources are display referred, and that reference encodes therefore apply **no** transfer. None of that survives. Sources are **log encoded** (ACEScct), the working space is **ACEScg**, plates are delivered scene linear and ungraded, and the references get a full ACES output transform. `docs/COLOR_AND_FORMAT.md` section 1 was rewritten rather than amended, because amending it would have left two readings in one document. **Anything written before 2026-09-11 that reasons about colour is suspect**, including the M3 reference encode, which applies no transfer and would produce a flat milky mp4 from a log source **Superseded a second time on 2026-09-11**, and the part that changed is the part this entry was most confident about: the plate is **graded** now, with the approved look from the colour session baked in, and there is no CDL anywhere in the tool. Read COLOR_AND_FORMAT section 1 and nothing in this row | ACEScg plate graded from the shot's CLF, sRGB reference. COLOR_AND_FORMAT section 1 | no, but M3 reopens |
| OQ-18 | The spec PDF marks HDRI and camData as **Required** per plate, but QC-050 and QC-051 are warnings, so a missing one does not block. Should they be errors? Severity is a studio policy call, not a spec reading | Keep as warnings | no |
| OQ-19 | ~~QC-026 severity~~ **RESOLVED 2026-09-10, REOPENED 2026-09-11 by the real tracker.** The resolution rested on a premise: timeline and files are both 24 in Resolve, so any other rate means a file escaped the conform. The tracker's FPS column says that stopped being true. Of 778 rows carrying a rate, 677 are 24 and **101 are not**, and it is not legacy noise: every one of the 370 rows on turnovers before number 90 is 24, while the recent turnovers carry **27 rows at 23.976 and 2 at 30** out of 262. Rows not yet on a turnover run wider still - 29.97, 30, 25, 60, 120. **What the sheet cannot say is whether that column is the timeline rate or the rate the camera shot at**, and only the first would make QC-026 fire. If it is the timeline rate then an error per non-24 shot would block roughly one row in nine of a current turnover, which is a rule that gets switched off rather than obeyed. Frame math is unaffected either way, since it follows the timeline and `frames.py` already carries 23.976 as 24000/1001. Answer by asking the editor what changed around turnover 90 **2026-09-19, the sample says which**: the Sony files are 24000/1001 (container, `tmcd`, audio sample count) and the shooter's Resolve timeline and every clip's `MediaFrameRate` are 24.000 with `IsForceConformed` true. So at least on this camera the column records the timeline's rate and the file disagrees with it. OQ-69 | error, as QC_RULES states, until someone says which rate that column is | no, but ask before M4.3 ships |
| OQ-20 | ~~Lens grid deliverable is not planned yet~~ **RESOLVED 2026-09-10 by the user: out of scope for v01, handled by hand.** The lens grid arrives as **a folder in the turnover package**, not as a clip on the timeline, which answers the part that was actually confusing: the scan was never going to meet one, so it cannot fail the shot naming regex and QC-010 was a red herring. For v01 the editor moves that folder to the delivery root and renames its files themselves, so the tool neither plans nor writes a lens grid, and the questions about which show to file it under and how to version it do not arise. What the tool still does: warns when a turnover has no lens grid folder (QC-054, reworded from "clip"), since the studio's sheet marks it Required, and says so when there is one (QC-057), since a manual step nobody is reminded of is a step that gets forgotten. `naming.lens_grid_png` and its parser stay, because they are the spelling the editor has to type by hand and the tool can still check it  **CLOSED 2026-09-22: Ben delivers the lens grid** (user). Not the editor by hand, and not the tool. QC-054 and QC-057 are retired with it, `naming.lens_grid_png` and its parser branch come out, and OQ-73(c) closes with it: a `lensgrid` in `Shot Type` is recognised and passed over rather than planned.| Manual in v01: the editor moves and renames it, the tool only tells them it is there | no |
| OQ-21 | **Alpha from a container source.** Still open, and now more likely to matter: ProRes 4444 carries a genuine alpha channel and it is the specified delivery format, where before it was one accepted option among several. The container path decodes `gbrpf32le`, which is RGB only, so an alpha would be dropped silently. Two things are still undecided and neither can be settled from the docs: what counts as a *real* alpha rather than the opaque one a codec always carries, and whether a plate consolidated out of Resolve ever legitimately has one. `MediaInfo` records no alpha field, so nothing could set the flag even if the decoder took one. Answer with a real turnover | RGB only from a container; decoding `gbrapf32le` is a one line change once there is something to switch on | no |
| OQ-22 | ~~No Mac to build or test on~~ **PARTLY RESOLVED 2026-09-10**: a GitHub Actions `macos-latest` runner (macOS 26, arm64, a standard runner drawing on included minutes) now runs lint, types and the full suite on Apple Silicon on every push, in `.github/workflows/ci.yml`. That was the larger half of the question: nothing had ever executed this code on the target, and the dev machine tests against Ubuntu's ffmpeg 6.1.1 while the product ships 9.0.1, so CI puts the bundled arm64 binary on PATH and exercises the shipping configuration. **Still open:** the M7 packaging job, since `build/build.py` and `proingest.spec` do not exist yet, and M8, which needs a real Mac regardless because the turnovers live on the editor's Drive and no runner can judge whether a reference encode looks right | CI covers correctness; a borrowed or rented Mac covers M5 UI work and M8 validation | M7 packaging only |
| OQ-23 | **VideoToolbox quality is uncalibrated.** Two halves. *Does the encoder open a session on real hardware?* **ANSWERED 2026-09-10 by CI**: `h264_videotoolbox` encoded 24 frames on a `macos-latest` arm64 runner and ffprobe read the result back as h264. (The runner is virtualised and logs a benign `IOServiceMatchingfailed for: AppleM2ScalerParavirtDriver`; it encoded anyway, so bare metal will be at least as capable.) *What does its quality scale mean?* **still open**: VideoToolbox has no CRF, takes `-q:v` on a 1-100 scale, and no published formula maps that to x264 CRF 18. A working encoder says nothing about how its output looks. Needs a real plate encoded both ways and compared | Default to software x264 CRF 18, which is what the spec pins; hardware encode opt-in and labelled an approximation until measured | no |
| OQ-24 | ~~Is every machine that will run this Apple Silicon?~~ **RESOLVED 2026-09-10 by the user: Apple Silicon only.** No Intel Mac is in scope for v01, so the arm64 bundle stands as it is and nothing needs a universal2 build or a second lock entry. **No runtime architecture guard is being added either**, deliberately: an arm64-only `.app` is refused by macOS itself on an Intel machine, with a clearer message than anything this tool could print, and the case where it would matter instead (running from source on an Intel Mac and hitting `Exec format error` at the first ffprobe call) is a developer's problem on a machine that is now confirmed not to exist. The user also intends a **Windows 11** build for v02, which is already the v02 backlog item and the reason `_platform_binary` still knows about `.exe` | Apple Silicon only in v01; Windows 11 is a v02 intention, not a v01 constraint | no |
| OQ-25 | ~~Where does Google Drive actually mount on the editor's Mac?~~ **RESOLVED 2026-09-10 by the user: the tool does not look.** The editor points at two folders, a **source root** and a **delivery root**, with a folder chooser each, and both happen to live on a Drive mount. Where that mount is stops being the tool's problem: nothing probes `~/Library/CloudStorage/GoogleDrive-*/My Drive` or `/Volumes/GoogleDrive`, and no path is assumed. The roots are remembered per batch, and a dialog opens at the last used folder. This also removes the first-run detection step and two lines from the Mac session checklist. **It does not settle FR-2**: the shooters may be on Windows, so an OTIO can still carry `G:\\...` media paths that mean nothing on the editor's Mac. Those are handled where they always were, by the path map or by the filename search over the chosen source root, which needs no configuration and is why the map is now optional rather than load bearing | Two user-chosen roots, remembered per batch; no mount detection anywhere | no |
| OQ-26 | **Which metadata does the AD and supervisor actually want in the pane** (FR-14, UI_SPEC section 12)? The field list is currently everything `core/models.py` holds, ordered by a guess at what matters. The section that will decide whether the pane earns its width is Side files, because parsed camData is the only place lens, filter and camera body ever appear in the UI, and its format is still OQ-11. Answer with a real turnover and one review session alongside OQ-4 and OQ-11. **Built to its default on 2026-09-12** (M5.6): nine sections, section 12.2's eight plus **Colour**, which M4.6 made possible after that table was written. The field list is a value rather than a layout - `ui/metadata.describe` returns `Section`s of `Field`s and the widget only draws them - so the review session can argue with a list and a changed answer is a changed list, not a rebuilt pane. `docs/MAC_SESSION.md` carries the line that says to hold that session in front of a real turnover | Show every field the model holds, grouped as in UI_SPEC section 12, sections hidden when empty. Ordering is cheap to change later; the pane reads the model and stores nothing | no; built to the default in M5.6 |
| OQ-27 | ~~Does a turnover's wav start where its picture media starts?~~ **RESOLVED 2026-09-10 by the user: yes, and it ends with it too.** If a clip has audio the wav runs cut point to cut point, the same span as the picture. `render._audio_skip` was built on exactly that assumption and is confirmed rather than changed. Two things follow that were not obvious. The wav is **not** longer than the delivered range, so extending Out into the handles outruns it, which is why the encode pads the audio: `-shortest` alone let a short wav truncate the picture, 24 frames asked for and 12 delivered. And QC-043 now has a firm expectation to check at scan time rather than a guess, since a wav that does not match the un-edited picture range means a malformed turnover | Audio and picture are co-extensive; the reference pads audio and never trims picture | no |
| OQ-28 | **How many handle frames should a turnover carry beyond the cut?** Possibly answered for free: the sidecar (OQ-33) is specified to carry `handles` per shot, which would give QC-030 a real number per row instead of one Settings guess for the whole batch. Worth confirming when the sidecar format is settled. QC-030 warns when there are fewer, and no doc has ever stated the number. PRD FR-5 says handles are already inside the timeline range and the extra frames exist only so Out can be extended, which says the expectation is real but not what it is. The default is a guess and it is a setting, so the cost of being wrong is one number in the Settings page. Answer with a real turnover: the answer is whatever the shooters' Resolve export actually leaves beyond each cut | 8 frames at each end, `qc.RuleSettings.expected_handle_frames` | no |
| OQ-29 | ~~Which ACES output transform do the references use?~~ **MOSTLY ANSWERED 2026-09-11**: the colour session is **DaVinci YRGB Color Managed, ACES 1.3**, so the tool pins an ACES 1.3 built-in config and does not track `studio-config-latest`. Matching the session matters more than being current, and a dependency bump must not change what a reference looks like with nothing in the log to say so. **What is still open** is the narrow half: which sRGB output transform *within* 1.3, since the config carries more than one and Resolve's own naming does not map onto OpenColorIO's one to one. Same shape of problem as OQ-34 and it should be answered in the same conversation with the colourist. **The config half is now built**: M4.5.1 pins `studio-config-v2.2.0_aces-v1.3_ocio-v2.4` in `core/color.BUILTIN_CONFIG`, the studio config rather than the cg one because it carries the camera vendor logs and the full view transform set. Only the output transform half is left, and M4.5.3 is where it bites. **Built to a default on 2026-09-12** (M4.5.3): `color.VIEW` is `ACES 1.0 - SDR Video` on `color.DISPLAY`, which is `sRGB - Display`. The three other views the pinned config offers on that display are not candidates - two are a D60 simulation and an un-tone-mapped debug view, and `Raw` is no transform at all - so what a real session would settle is whether Resolve was showing the same one, not which of ours to pick. A test asserts the name exists in the config, so a rename in a future ACES version fails at the first bake rather than in a delivered mp4 | ACES 1.3 built-in config, pinned in code; sRGB output transform confirmed against one graded shot | no; built to the default in M4.5.3 |
| OQ-30 | **Answered 2026-09-23 by Ben's real EDL (`Turnover199/Turnover199.edl`): source timecode.** It carries no `FROM CLIP NAME` on any event and reels every event `AX`, so neither the name nor the reel can identify an event; each event's source range sits inside exactly one file's timecode range, 24 frames in. Match by timecode containment, with the clip name required to agree where an EDL carries one, and ambiguity either way a must-fix (`docs/REVIEW_2026-09-23.md` chunk A). The history follows. **How does a CDL entry in the EDL match a row?** ~~Superseded 2026-09-11~~ **and un-superseded the same day**, which is worth recording because the question is easy to assume dead: it was set aside in the morning when the grade became a CLF named by a sidecar, and it returned in the afternoon when the grade went back to being a CDL inside an EDL. Nothing about it changed except which EDL, the shooters' then, Ben's final one now. An EDL event identifies itself by reel name, by `FROM CLIP NAME` comment and by source timecode, and different Resolve export settings populate those differently. **Matching on the wrong one silently applies a neighbouring clip's grade, which looks plausible and is wrong.** `timeline.py` already walks EDL events, so this is about which field to trust, not about parsing. Answer with one real EDL out of the colour session. **Built to this default on 2026-09-12** (M4.5.2): `clf.MATCH_FIELD` is the one field an event is trusted on, matched on the clip name without its extension and without case, and `ColorSession._event_by_reel` is the fallback, which wants the reel **and** a source range inside the media's own timecode before it will answer. Nothing returns a nearest candidate. What a real EDL out of the session would still settle is whether Resolve populates the field at all on this export, and the tests pin both routes so the answer costs one constant | Match on `FROM CLIP NAME` first, since that is already the contract the whole naming spec rests on, and fall back to reel plus source timecode. Raise QC-009 rather than guessing when neither matches | no, but it wants a real EDL |
| OQ-31 | **The colour session has never exported for this tool, and the whole pipeline rests on what it produces.** Reworded 2026-09-11: it used to be about the shooters' template project, and the shooters now matter less than the colourist does. Someone has to run one session end to end and confirm that Resolve exports a CLF that is genuinely `ACEScct in > CDL + look > linear ACEScg out` with no display rendering in it (QC-039), that the grade really was primary only, and that a sidecar comes out of it at all (OQ-33). Until that has happened once, every claim in COLOR_AND_FORMAT section 1 about what arrives is a specification rather than an observation. **One graded shot through the real chain answers OQ-29, OQ-33, OQ-34 and OQ-37 at the same time** and it is the cheapest thing on this list **2026-09-18: the 'CLF at all' part is answered, and the answer is no.** Resolve does not export CLF; it exports `.cube` through Generate LUT (OQ-54). The rest of this row stands with 'cube' read for 'CLF'. | Run one session on one shot alongside M4.5 and validate before M8 | no, but it blocks M8 validation |
| OQ-32 | ~~What do the four colour controls actually range over?~~ **CLOSED 2026-09-11 by the user: the colour controls are removed from the tool.** Colour is authored in the session and arrives as a CLF; the tool applies it and never adds to it. FR-16 keeps the three viewers and loses the sliders, QC-037 is retired, and UI_SPEC section 14 is rewritten | n/a, removed | no |
| OQ-33 | **What pairs a CLF with a shot?** Reopened 2026-09-11 with OQ-40, having been closed earlier the same day when the grade was briefly a CDL inside the EDL. The session exports one CLF per shot and something has to say which is which. A filename convention over the shot ID is the obvious answer and costs nothing, since the shot ID is already the contract the whole naming spec rests on; a sidecar is the explicit alternative. **The failure mode is the same as OQ-30's and it is the reason to care**: pairing the wrong CLF applies a neighbouring shot's grade, which looks entirely plausible. Answer with one real export from the session, which also answers OQ-29, OQ-30 and OQ-39. **Built to this default on 2026-09-12** (M4.5.2): `clf.index_clfs` reads the shot code out of each CLF filename and `ColorSession.clf_for` returns the one that names the row, None when the session delivered none, and raises rather than choosing when two name the same shot, because picking either is picking a grade. A CLF naming no shot is nobody's rather than an error. Still wants one real export to confirm the session names them that way **2026-09-18:** the file is a `.cube` (OQ-54); the pairing by shot code in the filename is unchanged. **2026-09-18:** pairing by shot code is now for the optional cube only; the CDL sits on the event the row already matched by clip name (OQ-30) | CLF filename contains the shot ID, matched exactly, and QC-009 when no CLF matches rather than a nearest guess | no, but it wants one real export |
| OQ-34 | **Resolve's IDT names do not match OpenColorIO's colour space names. BUILT TO ITS DEFAULT 2026-09-12** as `color.INPUT_TRANSFORMS` and `color.resolve_encoding` (M4.6.3): the config's own names and aliases resolve to themselves in any casing, three rows carry the names the shooters were given (`c-log3`, `bm film`, `davinci wide gamut`), `S-Log3` deliberately has no row because it names four colour spaces, and nothing resolves by prefix or by nearest miss. **What is still open is the string, not the mechanism**: whether what a shooter actually types lands on a row is OQ-44, and a fourth camera is one row. **REOPENED 2026-09-12**, one day after it was closed, because the sources went back to camera native log and the tool applies a per shot input transform again. What is now known that was not known when it was first written: the three encodings expected today are **S-Log3, C-Log3 and BM Film**, all three exist in the pinned config as `S-Log3 S-Gamut3.Cine` (or three siblings), `CanonLog3 CinemaGamut D55` and `BMDFilm WideGamut Gen5`, and each was built through `color.input_transform` on 2026-09-12 and produced a processor. So the transforms exist; **what does not exist is the mapping from whatever a shooter types into the clip metadata onto those names**, and "more may be added" means it must be a table rather than three branches. The two traps are specific and both are silent: **a curve name does not choose a gamut** ("S-Log3" names four colour spaces), and **the config carries one variant where the camera offers several** (LogC3 at EI800 only, BMD Gen 5 only, Canon Cinema Gamut only). An unrecognised or ambiguous name must block the deliverable, QC-046 and QC-047, never fall back. **Narrowed the same day by OQ-37**, answered yes: the CLF starts at the source encoding, so no plate needs this table. What still needs it is the **aux still**, which is delivered ungraded, never gets the CLF, and still has to reach ACEScg. That is a much smaller surface and a sharper one, because a mis-transformed colour chart is the worst failure the tool has: the chart is delivered precisely to be matched against, and it still looks like a chart. Text below is the original 2026-09-11 framing, kept because it was right. ~~**CLOSED 2026-09-11 before it was ever real.** It existed for one day, on the premise that sources were camera original and the tool applied an IDT per shot. The shooters deliver **one studio standard log encoding** (OQ-39), so there is one input transform, it is a constant, and there is no per shot name to map. Left here because the reasoning is worth not repeating: the original question was **Resolve's IDT names do not match OpenColorIO's colour space names.** The sidecar names an IDT per shot and the tool has to turn that string into an OCIO colour space. "ARRI LogC3" in Resolve is several distinct things in an ACES config, LogC3 is exposure index dependent where LogC4 is not, and a near miss is a wrong image that looks entirely plausible. A hand written map is the obvious answer and it is also exactly the kind of table that looks correct and is not. Answer against a real sidecar and one graded shot (OQ-31)~~ **Promoted back to a build requirement on 2026-09-12**, when the user asked for the option of multiple input transforms selected by the file metadata. So the table is built whatever OQ-46 says; what OQ-46 decides is only which chains it is applied on. Its rules: a table and not a search, no prefix or fuzzy matching, an entry naming a curve and a gamut together, and a new camera costing a row rather than a code path **2026-09-18: load bearing on every plate.** The grade is the CDL applied in ACEScct and this table is what gets each clip there, so QC-046 and QC-047 are errors on every plate, not only the aux still. Nothing about the table's rules moved | Explicit table keyed on what the shooters are told to write (OQ-44), resolved through OCIO's own names and aliases, refusing anything not in it | no, but it is being built |
| OQ-35 | **Frame numbers: 1001, or derived from source timecode plus handles?** The colour workflow proposes deriving them from TC; NAMING_SPEC section 5, COLOR_AND_FORMAT section 3 and QC-102 all pin 1001, and the shooters' own spec sheet is where 1001 came from. These cannot both hold. TC derived numbering is genuinely more useful for tracing a frame back to the source, and it is also a change to naming, the planner, the EXR writer and every test around them, and it breaks the one thing the vendor's tools already expect. Answer by asking the studio which their vendors read | Keep 1001. It is what the studio's own spec sheet says, and the source TC is already in every EXR header for anyone who needs to trace a frame | no |
| OQ-36 | **DWAA 45 or PIZ for a graded final plate?** DWAA is lossy and was chosen when the plate was ungraded and the vendor would grade it themselves. A plate that now carries the approved final look is closer to a master, and lossless PIZ is a defensible ask. DWAA 45 is roughly a tenth of a percent off per value and about four times smaller, and the delivery sits on a Drive mount where size is a real cost | DWAA 45, unchanged, and PIZ as a Settings option if anyone asks for it | no |
| OQ-37 | ~~Should the CLF start at camera log rather than ACEScct?~~ **ANSWERED 2026-09-12, yes, and it is the most valuable answer on this list.** The user confirmed: in camera log mode the colourist always starts from camera log, and in studio mode from DaVinci Wide Gamut. **So the tool applies no input transform at all.** The CLF is the entire transform from the delivered pixels to linear ACEScg, OQ-34's mapping table is not needed to render a plate, the tool and Resolve cannot disagree about what a camera log is because only one of them converts, and a camera nobody has mapped renders without a code change. ACEScct leaves the chain with it. **What it does not delete is the aux still** (OQ-45): a colour chart is delivered ungraded, never gets the CLF, and still has to reach ACEScg, so that one path still resolves the encoding to a colour space. Original framing below. ~~**REOPENED 2026-09-12 with OQ-34**, and for the same reason: the sources went back to camera native log, so there is a camera log in this pipeline again. **This is now the highest value question on the list and it is one question to one person.** If the colour session can export a CLF that already has the IDT at the front of it, the tool applies **no input transform at all** in camera mode: OQ-34's table is never built, OQ-44 stops being load bearing, and the tool and Resolve cannot disagree about what a camera log is because only one of them performs one. The cost is that a CLF is then camera specific and cannot be reused across sources, which nobody was going to do. **Ask the colourist before building anything in OQ-34.**~~ Text below is the 2026-09-11 closure, kept for the reasoning it contains. ~~**CLOSED 2026-09-11 with OQ-34**, and for the same reason: there is no camera log in this pipeline. The CLF starts at ACEScct, the source is the studio standard log, and the gap between them is one constant transform that is identity if the standard is ACEScct. The original question was: **should the CLF start at camera log rather than ACEScct?** As proposed the CLF starts at ACEScct, so the tool has to apply the IDT itself and match Resolve exactly, which is the whole of OQ-34. A CLF exported with the IDT already at the front of it deletes that: one transform per shot, no name mapping, and no way for the tool and Resolve to disagree about what an IDT is. The cost is that the CLF is then camera specific and cannot be reused across sources, which is not a thing anyone was going to do anyway. **Ask the colourist whether Resolve can export it that way** before building the IDT map~~ **REVERSED 2026-09-18 by OQ-46.** The tool applies the input transform again, into ACEScct, ahead of the CDL. The reasoning above was right for a grade file that started at the source encoding; a CDL is arithmetic in the timeline space and cannot start anywhere but there | n/a, answered yes | no |
| OQ-38 | ~~Does the tool still build the turnover stringout?~~ **ANSWERED 2026-09-11 by the user: no.** The colour session exports a reference QT with the look and burn-ins and that is the stringout. M6 is dropped, along with `core/stringout.py`, UI_SPEC section 8, QC-140, QC-141, OQ-12 and OQ-15. What it gives up is recorded in PRD FR-9: the tool's would have been the only stringout cut to the **edited** In/Out. The original question was: **does the tool still build the turnover stringout?** The colour session exports a reference ProRes QT with the look and burn-ins, and the shooters deliver an offline string-out, so M6 may be building a third one nobody needs. The argument for keeping it is that neither of those uses the **edited** In/Out: the tool's stringout is the only one that shows the shot list as the editor actually trimmed it, which is what goes to the vendor. The argument against is a whole milestone **REOPENED 2026-09-25 by the user: the tool builds a stringout too**, beside the one Ben renders in Resolve. Decided the same day: the cut is **the final saved EDL in Ben's turnover** (the approved ranges, not the editor's trims); an event with no reference mp4 (a reference still, a clip with no `Shot Type`, a skipped row) is **an ungraded slate**; the **burn-in layout copies the Resolve overlay** on `SECA0003_pl01_colorChart_01_raw_4k_v01.exr`, carrying source TC, a 1001 counter and the shot code; the name is `turnover###_MM_DD_YYYY_<shooter>_SO_v##.mp4` in `<show>/_reports/`; **audio as the EDL has it, from `pl` events only**. The Mac ffmpeg build lists FreeType, fontconfig and harfbuzz (vendor page), so `drawtext` is expected there; unconfirmed on a Mac | building | no |
| OQ-39 | **Which log encoding is the studio standard?** **ANSWERED 2026-09-12, then dissolved the same evening.** The answer was **DaVinci Wide Gamut / DaVinci Intermediate**, which is `DaVinci Intermediate WideGamut` in the pinned config and builds a transform there. **The question then stopped existing**, which is the more useful outcome: there is no studio standard *mode*. The encoding is named per clip in its own metadata (OQ-44), and DaVinci Wide Gamut is one more entry in the input transform table (OQ-34) that a clip's metadata may name, exactly like S-Log3. A turnover may mix a shooter on a house template with two on their cameras and nothing has to be told which in advance. What that deletes: a Settings mode, a batch-wide encoding value, and QC-038. Text below is the 2026-09-11 framing, and the sentence about ACEScct making the input transform identity is the part that did not survive. ~~Settled 2026-09-11: the shooters deliver **one** log encoding, the same on every file whatever anybody shot on, so the tool has one input transform and the camera specific work stays in the shooter's Resolve project. What is not settled is which encoding it is. **ACEScct is the one to push for**, because the colour session's timeline is ACEScct and its CLF starts there, so the tool would apply no input transform at all and there would be no stage left between the decode and the grade that could be silently wrong. Any camera vendor log adopted as the house format also works and costs one fixed transform. This is a question for whoever specifies what the shooters deliver, not for the colourist. **M4.5.1 is built against the default** and did not need the answer: the encoding is one OCIO colour space name in `core/color.DEFAULT_SOURCE_ENCODING`, validated against the config when a run starts, so a different answer costs one string and no code. What it does change is whether there is a transform between the decode and the grade at all~~ | `DaVinci Intermediate WideGamut`, as one entry in the table rather than a mode | no, answered and then dissolved |
| OQ-40 | ~~Is the grade a CDL inside the final EDL, or a CLF per shot?~~ **ANSWERED 2026-09-11, after going both ways inside one session: it is the CLF, and the CDL travels beside it doing a different job.** Ben's session exports the final EDL (conform, trims, and the CDL as `*ASC_SOP` / `*ASC_SAT` lines), a `.clf` per shot, and the stringout. **The CLF is what the tool applies**, because it carries everything a primary grade can contain, curves and log wheels and hue curves included, where a CDL carries only slope, offset, power and saturation and would arrive silently incomplete. **The CDL is recorded, never applied**: it goes in the EXR header as the readable version of the grade for anyone without an OCIO install. Where the two disagree the CLF is what is in the pixels. The original question was: **is the grade a CDL inside the final EDL, or a CLF per shot?** **2026-09-18: the carrier is a `.cube`, not a CLF**, because Resolve writes none (OQ-54). The reasoning here holds: a 65 point cube carries curves and log wheels where a CDL cannot, so it is still the file applied and the CDL is still the record. | CLF applied, CDL recorded. Both from the colour session | no |
| OQ-41 | **What should the stringout filename checker accept?** The tool no longer builds the stringout (OQ-38), but `naming.stringout_mp4` was kept so a name a human typed can still be checked. Measured against the 55 real names in the tracker, **it accepts none of them**. It builds `turnover001_02_23_2026_danielluckett_v01.mp4`; the real sheet's dominant form is `turnover106_08_19_26_ericscheid_SO.mp4` - a **two digit year**, an **`SO` token the spec never mentions**, and **no version at all** on 14 of the 55. The rest vary by case (`SO`, `so`, `V2`, `_V02`) and four are not filenames at all (`MultiMango`, `Artist Supplied`, `N/A`). A checker this strict would flag every correct file, which is worse than not checking, so it must not ship as it stands. The question is how tolerant to be: these names are typed by five different editors and the variation is the norm rather than the exception  **CLOSED 2026-09-22: "ignore stringout completely. Tool does nothing."** (user). The tool neither builds the stringout, nor transcodes it, nor renames it, nor reads it, nor checks a name anybody typed. So there is no checker to specify and the question has no subject. `naming.stringout_mp4`, its parser branch and their tests come out.| Accept `turnover<num>_<mm>_<dd>_<yy or yyyy>_<shooter>[_SO][_vNN].mp4` case insensitively, warn rather than error, and never rebuild a name the tool does not write | no |
| OQ-42 | **An EXR source is transformed as though it were the studio standard log.** Section 2 accepts an EXR sequence already in ACEScg or ACES2065-1, with a warning, and M4.5.4's plate branch applies the same `input_transform` to it that it applies to a log ProRes. For a source that is already scene linear that is a **wrong image rather than an error**: it looks like a grade. Three ways out. Refuse the delivery outright, which contradicts section 2. Read the source's own `chromaticities` and `proingest/colorspace` and pick the transform from them, which is the only one that needs no human. Or give the row a source encoding override, which is a per shot decision the whole design exists to avoid. Nothing is built; today the Settings encoding is the authority for every source, exactly as section 2 says it is for a container. **Cheaper as of 2026-09-12**: the third way out was rejected because a per shot source encoding is "a per shot decision the whole design exists to avoid", and camera mode makes the per shot encoding the norm. An EXR that names its own colour space is then just another clip whose metadata says what it is, and it takes the same route as a container whose metadata does. The header detection is still the work; what is gone is the architectural objection to where the answer would live | Detect it from the EXR's own header and transform from what it says, warning either way | no |
| OQ-43 | **The resize happens before the colour transform, not after it.** The chain diagram in section 1 draws the plate branch as CLF, then linear ACEScg, then resize; the code resizes in the ffmpeg decode, in the source's log encoding, and transforms after. Both orders are bounded, so nothing clips and the property the diagram protects still holds: **no scene linear pixel ever reaches swscale.** What differs is the edges. Resampling in log and resampling in linear ring differently on a high contrast edge, and neither is wrong so much as different. The current order costs one decode instead of a full resolution decode plus a numpy resample of every frame, which is most of the HD pass. Worth one measurement on a real 4k plate rather than an argument | Leave it; measure once on a real plate and record the number | no |
| OQ-44 | **BUILT TO ITS DEFAULT 2026-09-12** (M4.6.4) and still worth asking. The field is `scan.SOURCE_ENCODING_KEY`, **`Input Color Space`**, read from the OTIO clip's own metadata first and the container's tags second, and settable per scan. That name is a guess with a reason: it is Resolve's own Media Pool column for the input transform, so it is the field most likely to be filled in already rather than a new one somebody has to remember. **One real export from the shooters confirms or moves one string**, which is the same exercise as OQ-31. The rest of the row stands as written. **Where in the clip's metadata does the camera log live, and what exactly do the shooters write in it?** New 2026-09-12 with the move to camera native log. The user's words were that the shooters "write the correct LOG for their camera into the clip's metadata", and every part of that needs pinning down before code reads it. **Which carrier**: a Resolve Media Pool metadata field exported into the `.otio`, a tag written into the consolidated ProRes itself and readable by ffprobe, or a comment line on the colour session's final EDL. The tool already parses all three, so this is about which one Resolve actually populates on the export the shooters run, not about parsing. **Which field**: it has to be one named field, not "a comment somewhere", or the tool is guessing. **What string**: "S-Log3" names four colour spaces in the pinned config, so the instruction to the shooters has to demand the curve *and* the gamut, and the easiest way to guarantee that is to ask for the **Resolve input transform name verbatim** rather than a description of the camera. Whatever the answer, the tool maps it through OQ-34's table and refuses what it cannot resolve (QC-046, QC-047), because the failure is a wrong image that looks right. **Mostly answered for free by OQ-37**, which came back yes: a plate no longer needs this string to resolve to anything, so a missing or unrecognised one costs the EXR header a line of provenance rather than a wrong picture. It stays load bearing in exactly one place, the **aux still** (OQ-45), and blocking is scoped to that **2026-09-19, the sample**: the metadata CSV has no `Input Color Space` column, because that is a clip property and not a metadata field (Resolve scripting API), and it has `Input Gamma` = `Gamma 2.4` whose meaning is unsourced. The encoding sits in `Gamma Notes` and `Color Space Notes`, so `SOURCE_ENCODING_KEY` may have to become two fields joined. `docs/SAMPLE_TURNOVER_199.md` section 4, OQ-68 **Later the same evening**: the same CSV also carries the shot code (OQ-62), so the metadata export is the tool's identity record as well as its encoding record | One named metadata key, configurable in Settings, read from the OTIO clip metadata first and the container tags second, holding the Resolve input transform name verbatim. Missing or unresolvable blocks an aux still and is recorded elsewhere. **No batch-wide fallback**, since the encoding is a per clip fact; whether the UI should offer a per row override for a turnover that arrives with none is the one piece of this still undecided | no, and much smaller since OQ-37 |
| OQ-45 | **Should the colour session hand over the aux stills converted, so the tool never converts anything?** New 2026-09-12, and it is the last thread left after OQ-37. A colour chart, grey ball, mirror ball or size reference is delivered **ungraded** by design, so it never gets the shot's CLF, which leaves it as the **only** picture the tool transforms on its own authority. Everything else a turnover delivers is the colour session's work and can be checked against the session's own stringout; the chart is the tool's own conversion and nothing upstream has looked at it. That is uncomfortable for exactly the deliverable where being wrong is worst: a mis-transformed chart still looks like a chart, and a compositor matching against it has no way to tell. If the session can export the aux stills already in linear ACEScg, or a second CLF per camera that is the conversion alone, the tool's last transform of its own disappears and OQ-34 goes with it. Worth one question at the same time as OQ-31, and it is cheap to leave as it is if the answer is no | Leave it: the tool converts aux stills itself, with the mapping table scoped to that path and an unresolvable name blocking that deliverable alone | no |
| OQ-46 | **Does the colour session's CLF contain the input transform, or does the tool still apply one before it?** New 2026-09-12, and it is the one question whose two answers produce images that both look plausible. The user's answer to OQ-37 was that the colourist starts from camera log in camera mode and DaVinci Wide Gamut in studio mode, which reads as the CLF containing the conversion, so the tool applies the CLF alone on a graded plate. The same day the user asked for **multiple input transforms selected by the file metadata**, which is a facility that only changes a graded plate if the CLF does *not* contain the conversion. Both readings are reasonable and the table is built either way (OQ-34); what moves is whether that table is applied ahead of the CLF. **Applying both converts twice**, and the result looks like a grade decision rather than a defect: nothing errors, every check passes, and it is invisible until a compositor works against it. Applying neither where the CLF expects a working space is equally silent in the other direction. Confirm it against one real export, which is the same exercise as OQ-31, and until then the tool must record which case it believes it is in rather than inferring it per render **2026-09-18:** whether the grade file contains the input transform is now a question about a `.cube` from Generate LUT, and the answer is decided by the node graph it was generated from: `docs/COLOUR_SESSION_EXPORT.md` puts the Color Space Transform inside the graph so the cube contains it. One test export (OQ-54) confirms. **DECIDED 2026-09-18 by the user: the grade is the CDL in the final EDL, applied in ACEScct, and the tool applies the input transform the clip's metadata names ahead of it.** So the grade does not contain the input transform, by construction: Resolve's CDL export carries node one's primaries of a colour managed session, whose timeline is ACEScct by standard, and the numbers mean something only there. The tool converts into ACEScct, applies the CDL, converts to ACEScg. A cube from Generate LUT out of that same session is the clip's node graph ACEScct in and out and takes the CDL's place for a shot that needed more than the wheels. This reverses OQ-37 and is what the 2026-09-12 note here called converting around the grade; it is no longer a double conversion risk because there is one case. OQ-55 is the test export that checks the decision | The tool converts into ACEScct from the metadata, applies the CDL or the shot's cube, converts to ACEScg | no, decided; OQ-55 checks it |
| OQ-47 | ~~QC-039's scene linear probe is calibrated on ACEScct and the CLF no longer starts there.~~ **ANSWERED 2026-09-13, by building the replacement.** Raised 2026-09-12 while building M4.6.2. The probe fed 1.0, the top of the log range, through the CLF and called the result scene linear if it exceeded a fixed floor of 2.0. That floor was chosen when every CLF started at ACEScct, where white is worth 222 in scene linear. **Since OQ-37 the CLF starts at whatever the clip is encoded in, and white is not worth the same in each**: ACEScct and BMD Film Gen 5 give 222, DaVinci Intermediate 100, S-Log3 38.4, and **C-Log3 14.7**, so a dark C-Log3 grade answered under 2.0 and QC-039 refused a valid delivery - one of the three cameras named for this show. **The probe is now a ratio**: two samples, `LOG_WHITE` at 1.0 and `LOG_NEAR_WHITE` at 0.8, and how far apart they come out. A grade scales both and cancels, so what is measured is the slope at the top of the range, which is what a tone map flattens and what no exposure can move. **The numbers were re-measured rather than taken from this row**, and that changed one of them: the 0.9 sample this question proposed puts the lowest plate at 1.35 against a highest display rendering of 1.016, which no floor near 1.4 can separate, while 0.8 puts them at **1.86 and 1.055**. `TONE_MAP_RATIO_FLOOR` is 1.4, about a third from each in the log scale they are separated on. A chain that crushes the lower sample to nothing passes, because that is a dark grade rather than a tone map and QC-039 is an error **2026-09-18: the probe changed shape again, with the carrier.** A cube is now a grade alone out of an ACEScct session, log in and log out, so the ratio above would call every one a display rendering (1.0 over 0.8 is 1.25, under a floor of 1.4). The probe now measures the **step** between the two samples through the cube alone, floor 0.07 in ACEScct code value, measured against every encoding in play. QC-039 has the numbers | answered: a ratio probe at 1.0 and 0.8 with a floor of 1.4, replacing the fixed white floor | no |
| OQ-48 | **What should Scan do to a turnover that already has rows?** **Answered 2026-09-23 (user, D8 and D16): re-scan it and keep the editor's edits**, carried over by File Name (`scan.carry_over`, UI_SPEC section 15). New 2026-09-12 with M5.4. `Add Turnover` scans the folder it is given straight away, and `Scan` re-tries the turnovers that came back with **no** rows: a mount that was down, a timeline that had not finished downloading. A turnover that has rows is deliberately left alone, because a scan rebuilds rows from the timeline and the rows carry the editor's In, Out, shot code, notes and skip reasons, so re-scanning one silently discards a pass over a turnover. The useful version of this is a re-scan that keeps the overrides and reports what moved underneath them, which is a merge rather than a scan and wants QC-035's snapshot-versus-current machinery. Nobody has asked for it yet; a turnover is re-delivered rather than re-scanned in the workflow as described | Scan re-tries only turnovers with no rows, and a changed turnover is added again under a new id | no |
| OQ-49 | **What form should the user guide take, and does anyone but the editor edit it?** New 2026-09-12, with FR-17. The user asked for "a pdf or a file that can be pasted into google docs", which are two different products: a PDF is fixed and reads the same for everyone, a Google Doc is something the studio can correct, extend and share a link to, and it is also something that goes stale in a copy nobody can regenerate. The cheap answer is to keep **one** source in the repo, `docs/USER_GUIDE.html`, with the screenshots embedded, because that both prints to PDF and pastes into Docs whole, images included, in one step. What the answer changes is small but real: if the studio is going to edit their copy, the repo's version stops being the only one and every regeneration has to be a paste rather than a replace. Worth asking once, before the first hand-over rather than after | one HTML source in the repo, printed to PDF for handing over and pasted into Docs when somebody wants to edit it; screenshots regenerated by a harness rather than re-shot | no |
| OQ-50 | **Is the colour session one package per turnover, or one per batch?** New 2026-09-12 with M5.7.1, and it is a documentation disagreement rather than a doubt about the workflow. PRD FR-12 lists "colour session package location" under **Settings**, which is per user and per machine. PRD section 6 step 4 makes ingesting the session a step in the **user flow**, between Scan and Review. `QC_RULES.md` scopes QC-008 to a **turnover** and says in as many words that "one turnover can be waiting on colour while another renders", which a single per-user Settings path cannot express. **Built to the superset**: `Turnover.color_session_edl` records the EDL per turnover on the batch, for the reason UI_SPEC section 13 gives for the two roots - it is a record of what this work was rendered from, not a preference, so reopening a `.pibatch` restores it. With one turnover it behaves exactly like a batch-wide value, and the Settings Colour group keeps what is genuinely a preference: the input transform table overrides, the ACES config version, the output transform, and the folder the ingest chooser opens at. What would still change if the answer is "per batch" is one field and one action, not the rules | Per turnover on the batch, with Settings holding the defaults and the chooser's starting folder | no; built to the default in M5.7.1 |
| OQ-51 | **Is the timeline's rate the one its global start states?** New 2026-09-13 from the code review. An OTIO `RationalTime` carries its own rate, and `core/timeline.py` used to take `round(value)` of each clip's times unrescaled and to read the timeline rate off the duration, which is computed from whichever clip came first. Resolve conforms every clip to the project rate so the two never disagreed on a real export, but a clip left at the camera's rate would have contributed a frame number in the wrong units. The loader now resolves the rate from `global_start_time` first and rescales every time to it before rounding. | The global start's rate is the project rate; a timeline with none falls back to the duration's, then 24. | No |
| OQ-54 | **Resolve does not export CLF. What does the colourist export instead, and what does the first real export have to show?** New 2026-09-18, raised by the user: "I have no evidence that Resolve exports a .clf file type", and there is none. The Resolve 18.6 manual lists CLF among the LUT formats it **reads**; Generate LUT writes 17, 33 or 65 point `.cube` files and nothing else; and Resolve writes no `.cdl` or `.ccc` either, the CDL travelling as comment lines in an exported EDL (Timelines > Export > CDL), which is what the tool already reads. Every version of `COLOR_AND_FORMAT.md` before today assumed a CLF per shot, and OQ-31 had recorded 'confirm Resolve exports a CLF at all' as something one real session would settle. **Answered to a default the same day**: the grade file is a **65 point `.cube` per shot from Generate LUT**, named with the shot code, and the tool accepts `.cube` beside `.clf` in the same role (`clf.GRADE_EXTENSIONS`); nothing else in the chain moved, since OpenColorIO loads either through the same file transform and QC-039 probes either. **What is not answered is what a cube contains.** Generate LUT bakes the node graph and nothing outside it, so whether the cube starts at the camera encoding and ends in linear ACEScg depends on the session being set up as `docs/COLOUR_SESSION_EXPORT.md` describes: project unmanaged (DaVinci YRGB), a Color Space Transform from the clip's encoding to ACEScct as the first node, one from ACEScct to linear ACEScg as the last, and the viewing transform on the timeline node rather than the clip. A session run colour managed instead yields a grade-only cube, ACEScct to ACEScct, and the tool would have to convert around it, which is the double-conversion risk OQ-46 is about. **One test export settles it**: one shot graded in that setup, its cube and its EDL handed over, checked with QC-039 and against the session's own stringout **SUPERSEDED IN PART 2026-09-18, later the same day: the cube is optional.** The grade is the CDL in the EDL applied in ACEScct (OQ-46), the session is colour managed with an ACEScct timeline, and a cube is generated only for a shot the wheels could not do, ACEScct in and out, taking the CDL's place. The unmanaged setup this row and the export page asked for is withdrawn, since it would export an identity CDL. What stands: Resolve writes `.cube` and not CLF, the tool accepts either in the cube's role, and one test export is wanted, now as OQ-55 | A `.cube` from Generate LUT only for a shot that needs more than the wheels; ACEScct in and out; it takes the CDL's place for that shot | no |
| OQ-55 | **The test export for the CDL chain, decided 2026-09-18 (OQ-46). What has to be shown before a delivery depends on it?** The chain is built on four facts none of which has been observed from a real session: **(1)** the session's timeline is ACEScct, as the export page asks, and not ACEScc, since a CDL replayed in the other one grades the shadows differently and nothing errors; **(2)** Resolve's CDL export out of node one round-trips the wheels closely enough, given that Lift is approximated and Luma Mix must be 0, and that the tool applies it with OpenColorIO's default no-clamp style where the ASC specification clamps between SOP and saturation; **(3)** the tool's input transform for each camera (`color.INPUT_TRANSFORMS`, the pinned ACES 1.3 config) lands where Resolve's own input colour space for the same camera lands, since the two now convert the same clip independently and disagree silently if they differ; **(4)** a cube from Generate LUT out of a managed session is the grade alone, ACEScct in and out, which QC-039's step probe assumes. One export answers all four: one shot graded with the wheels in node one, the EDL, the stringout and a cube of that clip; the tool renders the shot through the CDL and through the cube and compares each reference to the stringout. `docs/COLOUR_SESSION_EXPORT.md` "The first time" is the brief | The chain as built: ACEScct, no clamp, the pinned config's camera transforms; the stringout comparison is what catches a miss | **yes, before a real delivery** |
| OQ-56 | **Sources are camera-native files, not ProRes 4444. Which cameras and modes, exactly?** New 2026-09-19, from the review the user asked for: the user stated the shooters never render ProRes 4444, leave footage in whatever their cameras shoot, and hand consolidated timelines to the colourist. That reverses OQ-3 and the premise of COLOR_AND_FORMAT section 2, QC-020 and QC-021, all built to ProRes 4444 12 bit full range. Researched: ffmpeg decodes every common compressed camera codec (H.264 and HEVC in any wrapper, ProRes, DNx, MPEG-2, image sequences) and **no RAW format** (BRAW, REDCODE, ARRIRAW, Cinema RAW Light, X-OCN, N-RAW, ZRAW, CinemaDNG, ProRes RAW), so nothing RAW may reach a turnover; a Blackmagic body must record ProRes or DNx. **Decided by the user 2026-09-19: 4:2:0 sources are allowed with a warning**, so QC-020 becomes a warning and QC-021 is rebased on the real formats. Whether EXR sources stay supported at all is open; nothing delivers them. `docs/REVIEW_2026-09-19.md` has the decodability table | Camera-native compressed sources; RAW refused; QC-020 warning; QC-021 rebased once the cameras are named | **yes for the respec**: the cameras and modes |
| OQ-57 | **There is no OTIO. The scan has to be built from Ben's EDL and the turnover folder.** New 2026-09-19. The user: the shooters export nothing for the tool, and OTIO was never planned; the colourist's CDL export, a right-click on the timeline, is the tool's sole source of trims and colour, and the tool produces no timeline. Today `scan.py` reads nothing but an OTIO. An EDL carries per event the clip name (`FROM CLIP NAME`), the reel, the source In and Out that are the trims, the record position and the CDL, which is enough for the shot list, the conform and the grade; aux stills and BTS clips are events like any other. What it does not carry is the input transform per clip, which is OQ-58. The CDL export's conditions apply to Ben's timelines: one video track, no transitions, no compound clips. Researched: Resolve's OTIO export carries no grade, so it could never have replaced the EDL **2026-09-19, later: the sample includes a `.drt`** (`docs/SAMPLE_TURNOVER_199.md` section 3), the DaVinci Resolve Timeline file that Media Management writes beside trimmed media automatically (Resolve 18.6 manual, "Media Management of Timelines Creates .drt Files"). It carries what an EDL cannot: the timeline rate (24.0), each clip's record position, source start and the 24-frame handles, the resolution and the consolidated file paths; and none of the grade. If the shooters hand it to Ben anyway (OQ-68), the scan has a second input beside Ben's EDL and the EDL's missing frame rate has a source | Rebuild the scan on the EDL plus the folder; drop the OTIO reader once nothing depends on it | **yes**: waits on OQ-58 and OQ-62 |
| OQ-58 | **Without an OTIO, where does each clip's input transform come from?** New 2026-09-19. Three candidates, none verified. **(a) The camera file's own metadata**: Sony writes `CaptureGammaEquation` (`s-log3`) and `CaptureColorPrimaries` (`s-gamut3.cine`) into XAVC files and Canon writes the equivalent; Resolve's own auto-tagging reads exactly this. Whether Resolve's Copy with trim keeps it, and whether ffprobe exposes it or a second reader is needed, is what the user's clip answers. **(b) A second export from Ben**: an ALE with CDL from the same timeline, or an OTIO of it, if either carries the clip's Input Color Space; unverified, and one export from Ben tests both. **(c) A table keyed on something in the filename or codec**, the last resort. Preference: (a), with `color.INPUT_TRANSFORMS` behind it and OQ-59's turnover default for a clip that names nothing **2026-09-19, later, the sample turnover (`docs/SAMPLE_TURNOVER_199.md`): (a) is out.** The trimmed MP4s carry no Sony metadata at all: no `rtmd` track, no XML, no `uuid` box, and ffprobe reads transfer, primaries and matrix as `unknown`; only the range flag survives. Copy with trim rewraps the file and strips it. **The encoding appears in exactly one place in the sample**: the Media Pool metadata CSV, as `Gamma Notes` = `S-Log3` and `Color Space Notes` = `S-Gamut3.Cine` on every clip, beside ISO, shutter and focus distance. The `.drt` names no colour space per clip (every decoded blob searched). So the carrier is a metadata export from the shooters' Resolve project, or those same two fields arriving in Ben's project and leaving in his export; how the CSV's fields got filled, and whether Resolve fills them itself, is OQ-68 **2026-09-19 evening, the user: the shooter types the encoding into `Gamma Notes` and `Color Space Notes` themselves**, so the carrier is settled as those two fields of Resolve's metadata export (OQ-68)  **2026-09-21: the join is verified and the tolerance is measured.** `f"{Gamma Notes} {Color Space Notes}"` resolves in the pinned config to `S-Log3 S-Gamut3.Cine` on all five sample rows, with no new `INPUT_TRANSFORMS` row. Order matters: the reverse order and Resolve's own `S-Gamut3.Cine/S-Log3` both refuse, and either field alone names four spaces. How much sloppiness is safe is now a proof rather than a preference: casefolding and stripping every non-alphanumeric character is **injective over the pinned config**, 54 spaces and 166 names and aliases, **zero collisions**, so case, punctuation, spacing and field order can all be forgiven with no chance of selecting the wrong space. What cannot be forgiven is a valid name for the wrong thing: a shooter who means `S-Gamut3.Cine` and types `S-Gamut3` resolves silently, and measured through the full chain to ACEScg on frame 30 of `C0148.MP4` that is **2.17% mean, 8.0% at the 99th percentile, 41.8% peak**, worse than the BT.601 matrix bug of OQ-60. No parser can catch it, because it is a correct spelling of a different space, and a nearest-match rule would make it more likely rather than less. The CSV's own `Camera Manufacturer` and `Camera Type` can catch it, as a warning that the resolved encoding does not belong to the camera the row names; that rule needs an ID in `docs/QC_RULES.md` | The two Media Pool metadata fields `Gamma Notes` and `Color Space Notes`, joined in that order and matched after casefolding and stripping punctuation; exact match or QC-047, never a nearest miss | **yes**: OQ-68, who fills those fields and whether the export is part of the delivery |
| OQ-59 | **A per-turnover default input transform, set by the editor, for a shooter who left the camera transform out.** Required by the user 2026-09-19. Design: a field on the turnover in the batch file (a record of what it was rendered with, like the delivery root and the colour session, not a Settings preference); precedence is the clip's own metadata first, the turnover default second, else QC-046 stays an error; a row rendered through the default says so in QC-048, the spreadsheet's encoding column and the EXR header as a third `source_encoding_origin`, which COLOR_AND_FORMAT's EXR metadata section anticipated; the pulldown lists the rows of `color.INPUT_TRANSFORMS` plus S-Log3 with its gamut spelled out and "none", set on the turnover heading and in the metadata pane, re-running the row rules on change. **Two decisions are the user's and are open**: whether the default overrides a clip whose name the table cannot resolve (QC-047), or applies only to a clip that names nothing (the proposed answer, since a misspelt Sony clip falling back to a Canon default is the silent wrong plate the table exists to prevent); and whether one turnover is one camera, since a single default cannot cover a mixed turnover | Applies only where the clip names nothing; one camera per turnover | **yes**: the two decisions |
| OQ-60 | **Decided provisionally 2026-09-23 (user): BT.709**, on the decode and on the reference encode, which the review found converted with ffmpeg's BT.601 default while tagged BT.709. If it proves wrong it is switched in the next version (`docs/REVIEW_2026-09-23.md` D17, chunk G). **Built 2026-09-23, chunk G**: every decode states `in_color_matrix` and `in_range` (the file's own when it states them, else BT.709 and limited), the reference states `out_color_matrix=bt709:out_range=limited`, and QC-018 says which was used. Measured on ffmpeg 6.1: untagged BT.709 red decodes to 0.90 red the old way and above 0.97 now; Y of a pure red reference is 63, BT.709, where the old encode measured 81, BT.601. The history follows. **The decode asserts no range and no matrix, and QC-018 was never built.** New 2026-09-19, from the code audit. `ffmpeg.decode_command` passes no `-color_range`, no `in_range`, no matrix; `media.py` reads none of ffprobe's colour fields; COLOR_AND_FORMAT, the PRD and QC-018 describe a forced decode and a check that do not exist (`qc.py` says QC-018 is not raised anywhere). With ProRes this would have been a coin toss; with camera-native files it is less dangerous, because Sony states S-Log3 is recorded full range in XAVC and Canon records Canon Log full range with a range tag, and ffmpeg honours the flag. It is still an assertion the tool must make: pass the range and matrix explicitly from what the file declares, and raise QC-018 when the file declares nothing or contradicts the encoding **2026-09-19, later, measured on the sample (`docs/SAMPLE_TURNOVER_199.md`).** The range flag survives the trim and says full (`color_range=pc`, container and VUI). The matrix is unspecified, and **ffmpeg's default decode of these 3840x2160 files is BT.601**: a plain decode to 16 bit RGB is identical to an explicit `in_color_matrix=bt601` and differs from `bt709` by up to 815 of 65535 across 97% of pixels (frame 30 of `C0148.MP4`). If Sony encodes S-Gamut3.Cine with Rec.709 coefficients, which a third-party capture of a Sony body's acquisition record (`CodingEquations rec709`) suggests and Sony's own document has to confirm, today's decode is a matrix off on every plate and nothing reports it **2026-09-19, later: the matrix was almost certainly never there, and Resolve did not drop it.** The user asked why Resolve would strip it, and the bitstream answers: the H.264 sequence header of the trimmed file carries `colour_description_present_flag` = 1 with `colour_primaries`, `transfer_characteristics` and `matrix_coefficients` all written as **2, "unspecified"**, and `video_full_range_flag` = 1. That is a value an encoder wrote on purpose, not a field a rewrap lost, and it sits inside the compressed stream that Media Management copies without recompressing (Resolve manual: trim of H.264, XAVC and AVC-Intra "without recompressing or transcoding"). The three fields travel together under one flag in H.264 Annex E, and there is no standard code point for S-Gamut3.Cine primaries or the S-Log3 curve, so a camera writing S-Log3 has nothing true to put in two of the three; writing all three as unspecified is the expected result. Sony's technical summary confirms the other half: S-Log3 is recorded full range in XAVC with no legal-range option. The header's own timing is 1001/48000, 23.976, which is the camera's and not the 24.000 project's, another sign the stream is untouched. **Unverified until the original file is seen**: that its header reads the same, and which Y'CbCr matrix Sony used; a third-party dump of a Sony body's acquisition metadata shows `CodingEquations: rec709`, and the original's `rtmd` track would state it for this camera | Pass `in_color_matrix` and `in_range` on every decode, from the file's flags where it states them and from the encoding table where it does not, and raise QC-018 when neither says. Sony's coefficients want Sony's document | **yes, before any Sony plate is delivered**: the matrix is measured wrong today |
| OQ-61 | **What does Copy with trim actually produce?** New 2026-09-19. Researched: Resolve has trimmed temporally compressed codecs (H.264, XAVC, AVC-Intra) without recompressing since version 15; a 16.2 forum report found XAVC-I MXF copied whole rather than trimmed. Unverified and material: whether the trimmed file keeps the camera's start timecode (the conform depends on it, OQ-65), whether it keeps the gamma and gamut metadata (OQ-58), whether the range flag survives (OQ-60), and whether the shooters have "consolidate multiple edit segments into one media file" on, which decides whether two shots from one take are one file or two **Answered by the sample 2026-09-19 (`docs/SAMPLE_TURNOVER_199.md`)**: one file per timeline clip, filename unchanged, the camera's timecode kept and offset by the trim, **24-frame handles on both sides** (one second at 24; short where the source ran out), every camera metadata track stripped, a stereo 16 bit PCM track written into every file including the three stills whose originals the CSV lists with no audio, and a `.drt` written beside the media. The consolidate-segments question stays open: the sample has one segment per take | As the sample shows; segments per take still unknown | no longer for the file; the shooters' media management settings still |
| OQ-62 | **Clip names carry their extension, and the code assumes they do not.** Verified by the user 2026-09-19. `naming.parse_clip_name` is anchored with no extension, `DirectoryIndex.media_matching` compares the file stem to the raw clip name, and `ColorSession.event_for` compares the `FROM CLIP NAME` stem to it. Fix: strip the extension before parsing and matching. Still open: what the consolidated **filename** is against the timeline **clip name**, since Resolve's media management does not rename files and the naming convention renames clips; and what Resolve writes in the EDL's reel column for these clips. The clip answers the first; Ben's EDL the second **The sample answers the filename question and raises another, 2026-09-19 (`docs/SAMPLE_TURNOVER_199.md`).** The consolidated filename is the camera's own (`C0145.MP4`) and the timeline clip `Name` in the `.drt` is identical, extension included. **The naming convention was not applied to clip names in this sample**: `parse_clip_name` returns nothing for all five, and the shot code sits in the Media Pool `Shot` metadata field (`TEST0001`, with `Scene` = `Reality Burst`); nothing anywhere carries the element type. `MediaReelNumber` is empty in the `.drt`. Whether the shooters will rename clips per NAMING_SPEC section 1 at all, or the code will travel in metadata, is now the question; Ben's EDL still answers the reel column **The user, 2026-09-19 evening: the shooter renames the clips on the timeline and the consolidated clips hold the new names; that is the naming convention as intended.** The sample does not show it, since its clips kept their camera names on the timeline and on disk, so whether Media Management writes the timeline clip name into the consolidated **filename** is still unverified by a file (a third-party tool exists for exactly that renaming, which is a hint the other way). One consolidated sample with renamed clips settles it, and also shows what the CSV's `File Name` column then carries, which is what the tool would match rows on **Corrected by the user later the same evening, 2026-09-19: the clips are NOT renamed.** The camera filenames are what the shooters deliver, on the timeline and on disk, exactly as the sample shows, and **the shot code lives in the metadata**, the Media Pool `Shot` field, which reaches the tool through the metadata CSV (`Shot` = `TEST0001` in the sample). So NAMING_SPEC section 1's input contract moves from the clip name to a metadata field, `parse_clip_name` is applied to a metadata value rather than a name, matching media to CSV rows is by `File Name`, and Ben's `FROM CLIP NAME` will carry the camera name, which matches the file as it is. The extension question dissolves: nothing parses the filename any more. What the sample does not show is where the **element type and index** and the **aux still type** live, since every one of the five rows says `TEST0001` and nothing else: OQ-70 | Shot code from the `Shot` metadata field via the CSV; media matched to rows by `File Name`; filenames never parsed | **yes**, OQ-70: the field for the element type and the still type |
| OQ-63 | **Partly answered 2026-09-23 by Turnover121, which holds a clean plate with two `M2 000.0` freezes.** A freeze is rendered: the one frame as an EXR, a reference holding it five seconds, no audio (user). Any other `M2` speed is refused as QC-073. Compound clips are still unread. **Retimes, reversals, freeze frames and compound clips render wrong frames silently.** New 2026-09-19, from the code audit: nothing reads a speed effect, and a nested clip's range is relative to the wrong parent. The user confirmed none of these ever occur, so the shape is a **refusal**, not support: in an EDL a retime is an `M2` motion line and a reversed clip a negative speed on it; the EDL-only scan (OQ-57) refuses an event carrying one and says so | Refuse an event with a motion effect; nested clips cannot appear in an EDL | no |
| OQ-64 | **Google Drive mode on the editor's Mac.** The user believes mirror, 2026-09-19. Recommended mirror, with the source root and `_color` kept available offline: streaming mode hands the tool placeholder files that download on first read, the tool reads every frame of every shot, and nothing in the code can tell a placeholder from a file (the PRD asks for that and it is unbuilt). Ben's own setting only affects whether his exports appear promptly. Unverified until the editor's Mac is seen (M8) | Mirror; placeholder detection unbuilt | no |
| OQ-65 | **The approved cut is dropped silently when the media has no timecode, while the CDL is still applied.** New 2026-09-19, from the code audit. `clf.ingest` writes the CDL unconditionally and the In/Out only when `approved_in_out` finds a timecode on the media; the row is counted as matched and no report list says the cut was not applied. The result is the shooters' In/Out with Ben's grade. Fix: report it, and refuse to count the row as conformed. Related: `media.py` parses the timecode string at the timeline rate while the frame count uses the file's rate, so a file at another rate has a wrong start timecode; QC-026 blocks that today, and OQ-19's premise that frame math is unaffected by a downgrade is not true of the timecode origin | Report a matched row whose cut could not be applied; keep QC-026 an error until the timecode origin is rate-correct | no |
| OQ-66 | **COLOR_AND_FORMAT section 4 promises letterboxing for a non-4k source with QC-024; the code squashes, and QC-024 is unbuilt.** New 2026-09-19, from the code audit. `render._fit` scales to the target size, `qc.py` says so in a comment, and the non-16:9 case section 4 describes is checked only as exact resolution equality. PROGRESS records QC-024 as unbuilt; the spec and the `allow non-4k` setting do not. Decide: build the letterbox, or correct the spec to say a non-4k source is refused or squashed | Correct the spec to what the code does until somebody wants letterboxing | no |
| OQ-67 | **Media matching is case sensitive where EDL matching is not.** New 2026-09-19, from the code audit. `scan._index_entry_for` and `DirectoryIndex.by_stem` compare stems exactly; `clf.event_for` and `media.remap` casefold. On APFS or a Drive mount a clip whose name differs in case from its file matches Ben's EDL and not its media. Fix: one comparison rule, casefolded, everywhere a name meets a filename | Casefold every name-to-file comparison | no |
| OQ-53 | **Where does the colourist export a turnover's session to?** New 2026-09-17. The user described the flow: a fresh Resolve session, each shooter's turnover imported from the shared mount as its own timeline, and the final EDL and the CLFs saved "in a folder created for the session". The tool's chooser handles any location, but a location known in advance lets the scan find the session and offer it, which removes the one step of the flow an editor has to know to do. **Built to a default the same day**: `clf.find_session` looks for a folder named **exactly as the turnover folder**, first under the Settings folder the chooser opens at, then in a `_color` folder beside the turnovers on the mount (`<source root>/_color/<turnover folder name>/`), and wants exactly one `.edl` in it at any depth. The turnover's own folder name is the convention because it is the one string the editor, Ben and the tool already share. What a person still has to settle with Ben: that they export one timeline per turnover into a folder of that name, and whether `_color` beside the turnovers is somewhere they can write. If it is not, the Settings folder is the override and needs no code **2026-09-18:** read `.cube` for CLF throughout; the folder convention is unchanged. **DISSOLVED 2026-09-22 by the user: "remove `_color` folder stuff".** There is no colour folder and no discovery. The EDL and the CSV arrive in the turnover folder with the media (OQ-74), so the one convention this question existed to settle has no subject left. `clf.find_session`, the `_color` fallback and the post-scan offer built for it in M5.13 come out of the code, and `docs/COLOUR_SESSION_EXPORT.md`, `docs/WORKFLOW.md` and `docs/UI_SPEC.md` no longer mention the folder. | `<Settings folder or _color beside the turnovers>/<turnover folder name>/` holding one `.edl` and the CLFs; the button for anything else | no, but it wants a word with Ben |
| OQ-52 | ~~What should `CFBundleIdentifier` be?~~ **ANSWERED 2026-09-13, the same day it was raised.** It was raised because PACKAGING.md specified `com.<studio>.proingest` and no studio name appears anywhere in this repository, so the build shipped a placeholder. The user settled it in the other direction: **the studio is not to be named in anything that ships.** That costs nothing, because the premise behind the question was wrong in a useful way - an identifier does not have to name its owner, it has to be **unique and permanent**, and **Apple does not verify that whoever registers one owns the domain in it**, so reverse-DNS is a convention for avoiding collisions rather than a claim of ownership. Settled as `io.github.shango.proingest`, the standard form for a project whose only namespace is its repository (`github.com/shango/ProIngest`): unique because GitHub usernames are, genuinely controlled, and naming only the account that already owns the repo. **The half of the question that still binds is the second half**: do not change it once a build has reached the editor, because macOS keys folder access grants and its own application register off it, and notarizing under OQ-9 would bake it in. Settings, window state and logs are unaffected either way, being built from the application name rather than the identifier | n/a, answered | no |
| OQ-68 | **The sample's two documents, the `.drt` and the metadata CSV: are they part of what the shooters hand over, and who filled the CSV's camera fields?** New 2026-09-19 from `Turnover199` (`docs/SAMPLE_TURNOVER_199.md`). The `.drt` is the file Media Management writes beside trimmed media (manual), so it exists whether or not anyone exports it. The CSV is a Resolve Media Pool metadata export whose rows describe the **original** clips and whose `Gamma Notes` / `Color Space Notes` name the encoding the tool needs. **Provenance unverified**: the SLogMetaRaw README and a Blackmagic forum thread say Resolve reads Sony acquisition metadata itself only from MXF and ignores it in MP4, and two community scripts write exactly these fields, `deric/DaVinciResolve-metadata` into `Color Space Notes` (optionally setting the `Input Color Space` clip property to `S-Gamut3.Cine/S-Log3`) and SLogMetaRaw from the `rtmd` track into Media Pool fields and the CSV export. Also from the sample: what `Input Gamma` = `Gamma 2.4` reports; whether five video tracks with one clip each is how the shooter cut it or how the export writes (the CDL export wants one track); and one original camera file or its `M01.XML` to see the `rtmd` metadata and the camera's own rate **The user answered the first two on 2026-09-19, evening**: Resolve itself exports the CSV (Media Pool metadata export), and **the shooter types the input transform into the metadata by hand**, `Gamma Notes` and `Color Space Notes`, along with other fields that do not come from the camera file. So the two fields are a deliberate shooter step and the carrier for OQ-58, and the export is part of the delivery. An original camera file is offered and wanted (the SPS colour flags and the camera's rate). Still open: `Input Gamma`, the five tracks, and whether the `.drt` is handed over deliberately or only because Media Management writes it **Later the same evening**: the CSV is now load bearing twice over, for the encoding (OQ-58) and for the shot code (OQ-62), so it is part of the delivery by necessity, and the scan cannot build a row without it  **2026-09-21, the user: ignore `Input Gamma`.** The reader skips the column and that part is closed. The `.drt`'s project config was decoded and searched the same day and carries only RAW decode parameters and a target display list, so the files could not have answered it either. Also read out of `project.xml` that day: the shooters are on **Resolve 21.1.0.0017** (`DbAppVer`), which is what decides the metadata field set and what the export writes | Read the encoding from `Gamma Notes` + `Color Space Notes` when a metadata CSV sits in the turnover folder, and the timeline from the `.drt` when one does; otherwise QC-046 and OQ-59's default | **yes**: the user, on all four |
| OQ-69 | **24.000 in Resolve against 24000/1001 in the file: which did the camera shoot, and which does the tool trust?** New 2026-09-19 from the sample. The trimmed files are 24000/1001 three ways (time base 1/24000 with frame duration 1001, `tmcd` 24000/1001, and 560560 audio samples for 280 frames); the shooter's timeline, every `MediaFrameRate` and the CSV say 24.000, with `IsForceConformed` true on every clip. Today `media.probe` lets the timeline rate win and keeps the file's as `stated_rate` for QC-026, which was written for exactly this, but without an OTIO nothing carries the timeline rate: an EDL states none and the `.drt` states 24.0 (OQ-68). What the camera was set to, whether the shooters' project imposes 24, and what the tool should read the EDL at, are the questions **The user, 2026-09-19 evening: the shooters set every clip to 24.000 in Resolve from the camera's 23.98, then Copy with trim; the tool's outputs must be even 24.** Assessment: the conform is right and should stay. In Resolve it makes a 23.976 clip frame-for-frame on a 24 timeline, so Ben's EDL maps one source frame to one file frame; an unconformed clip on a 24 timeline would be retimed by Resolve's mixed-frame-rate handling and the mapping would break. The conform does not reach the file, which stays 24000/1001, and that is harmless: the tool already treats the timeline rate as authoritative, passes `-fps_mode passthrough`, and writes references at the project rate, so a 23.976 file becomes an even-24 deliverable with every frame intact. What has to change is QC-026, which today calls this an error (OQ-19), and the audio: the file's audio is real-time at 23.976 and the tool never resamples, so at 24 it runs 0.1% long and is trimmed to picture, a drift under half a frame on any shot shorter than 40 s. Whether to resample it, or leave it, is a small decision for the respec | Timeline rate wins as today, read from the `.drt` when present and from a per-turnover setting otherwise; QC-026 reports the file's rate | no, but ask before a real delivery |
| OQ-70 | **Which metadata field carries the element type and index, and the reference still type, now that the shot code is metadata?** New 2026-09-19, evening, from the user's word that clip names stay as the camera wrote them and the shot code is in the metadata. The deliverable names need three things per clip (`NAMING_SPEC` section 1): the shot code (`TEST0001`), the element type and index (`pl01`, `cp01`, `wit01`), and for a still its type and index (`colorChart_01`, `greyBall_01`, `BTS_01`). The sample's CSV carries only the first: every row, plates and stills alike, says `Shot` = `TEST0001`, `Scene` = `Reality Burst`. Candidates, none verified: the `Shot` field carries the whole convention string (`TEST0001_pl01`, `TEST0001_pl01_colorChart_01`), which the existing parser reads unchanged; or separate Resolve fields (`Take`, `Angle`, `Description`, `Keywords`, `Comments`) carry the parts, which is a mapping the shooters have to be told and the export has to include. Also: whether the export always carries the rows for the stills and BTS clips, and whether one CSV per turnover folder is the rule **Checked against the sample on the user's question**: none of the CSV's 37 columns, nothing in the `.drt` and nothing in the MP4s says plate, clean plate or chart; the five rows differ only in filename, timecode, duration and audio. The one signal that separates a still from a plate is the timeline `Duration` of 1 frame in the `.drt`, which says still and not which still  **ANSWERED 2026-09-21 by the user**: the shooters fill the Resolve Media Pool **`Shot Type`** field with the element code, `pl` plate, `cl` clean and so on, and `Shot` keeps the shot code alone. A bare code with no index means `01` (`pl` is `pl01`); each shot code has exactly one of each reference still, so a bare `colorChart` is `colorChart_01`; `pl02` and `cp02` still occur. Parsing is **case insensitive**, because the shooters do not keep the camel case, and output always uses the spec's spelling. Verified 2026-09-21: `Shot Type` is a real field in Resolve's Shot & Scene metadata tab. Two things are not verified. The only public report of the field is a Blackmagic forum thread in which Media Pool filtering does not see it, and `Turnover199` cannot show that it reaches the CSV, because the field was empty there and the export writes only populated columns. **One export with `Shot Type` filled settles both.** Which code a clean plate carries is OQ-72 | `Shot` carries the shot code and `Shot Type` the element code; a bare code means index `01`; parsed casefolded, emitted in the spec's spelling | **yes**, narrowed: OQ-72, and one export with the field filled |
| OQ-71 | **An AMF beside the EDL, carrying the exact input transform and the grade.** Raised by the user 2026-09-21 as a direction and **deferred the same day: the EDL stays, and the CDL stays in it.** Recorded because it would remove the hand-typed encoding of OQ-58 entirely. Verified 2026-09-21. AMF is Academy Specification S-2019-001, XML, carrying `inputTransform`, one or more `lookTransform` (an ASC CDL embedded directly, or a reference to an external CLF), `outputTransform`, the working space and the ACES version, which is a superset of what the tool reconstructs today from two CSV fields plus a CDL. **Resolve already exports it**, as of 19.0.1, with `Source Master`, `Dailies Request` and `VFX Request` presets, the last being exactly this use case. **OCIO does not read it**: 2.2 shipped only a prototype Python tool, `pyocioamf`, converting AMF to CTF through a prototype config, which does not belong in a delivery path; the reader would be ours, and it needs three values out of an XML file. The mapping from an AMF's URN to a colour space has a clean answer: **the pinned config carries no AMF transform IDs at all**, and only `studio-config-v4.0.0_aces-v2.0_ocio-v2.5` does (34 transforms, 69 directional entries). Upgrading to that config is not free. On frame 30 of `C0148.MP4` the plate path is **bit-for-bit identical** to today for both ACEScct and ACEScg, but v4 **deletes the `ACES 1.0 - SDR Video` view** that `color.VIEW` names, so the reference mp4 path breaks outright, and its nearest ACES 2.0 replacement differs by 3.3% mean and 11% peak in display code. So the shape, when it is built, is to **borrow v4's URN table and keep the pinned config**: only three v4 spaces are absent from the pinned one and none is an input transform. **The unverified risk**: every ID in that table is a v2.0 `CSC.*` URN, while an ACES 1.x AMF carries v1.5 `IDT.*` URNs, which are not in it, so one real AMF exported from Ben's Resolve decides whether the table resolves anything at all. AMF describes camera encoding to ACES and says nothing about the Y'CbCr matrix, so it does not touch OQ-60 **REOPENED 2026-09-25 (user): Ben will export a per-clip AMF from his grading session.** First use is diagnostic, read by hand against what the tool assumes per clip: which input transform Resolve applied (the Turnover121 files are tagged BT.709 while the CSV says Apple Log), which space the CDL is applied in, and whether any look sits beside it, since the slope of 4.886 renders white. Whether the AMF then replaces the CSV's colour fields or only cross-checks them is decided after that. Nothing is built until a real AMF is in hand | EDL and CDL stay the carrier until a real AMF has been read | no |
| OQ-72 | **`cl` or `cp` for a clean plate: which does a shooter actually type in `Shot Type`?** New 2026-09-21, from the answer to OQ-70. The user's instruction says `cl` is clean. The shooters' own spec CSV names every clean plate deliverable **`cp01`** (`MELT0001_cp01_raw_4k_v01.exr`), and `naming.ELEMENT_TYPES` is `("pl", "cp", "el", "wit", "re")` with no `cl` anywhere. Most likely these are two different things: `cl` typed on the way in, `cp` written on the way out, in which case the fix is an input alias and not a change to the output spec. Build accepts both, because accepting both is strictly safer: if the shooters type `cp` the alias costs nothing, and if they type `cl` while only `cp` is accepted then every clean plate in every turnover fails to name  **ANSWERED 2026-09-21 by the user, who corrected themselves twice and landed on the spec.** It is **`cp`**. There is no `cl`: the earlier statement that a shooter types `cl` was withdrawn, and the shooters' resources guide lists `pl`/`pl01`/`pl02`, `cp`/`cp01`/`cp02`, `lensgrid`, `colorChart`, `mirrorBall`, `greyBall`, `sizeRef` and `BTS`. No alias is needed and none is built: the code a shooter types is the code the deliverable carries. What that list raises instead is **OQ-73** | `cp`, matching the deliverable spec; no alias | no, answered |
| OQ-73 | **Two gaps in the `Shot Type` vocabulary the shooters' guide lists.** New 2026-09-21. **(a) `el`, `wit` and `re`** - element plate, witness cam, recon plate - are absent from the user's list of codes but are live rows in the shooters' deliverable spec CSV, all marked "If Needed", and all three are in `naming.ELEMENT_TYPES` today. Retired, or merely rare enough that nobody listed them? Dropping them silently would refuse a witness cam the spec says is deliverable; keeping them costs nothing. **(b) `raw`** is in the user's list. In the deliverable spec `raw` is not an element code but the token separating the EXR sequence from the reference video (`_pl01_raw_4k_` against `_pl01_ref_4k_`), and `ref` does not appear in the list beside it. So either `raw` carries a meaning in `Shot Type` that has not been described, or it is the deliverable qualifier listed among the clip types by accident  **(a) ANSWERED 2026-09-21**: the user omitted `el`, `wit` and `re` by accident, so all three stay. **(c) added the same day**: `lensgrid` also appears in the user's list, which would make it a clip on the timeline and reverse **OQ-20**, whose whole point is that the lens grid arrives as a folder in the turnover package and never as a clip, which is why the planner has an empty row for it and the editor moves it by hand in v01. If it can be a `Shot Type` the tool should deliver it like any other clip. (b) and (c) remain | `el`, `wit` and `re` stay; `raw` is the deliverable qualifier, not a `Shot Type`; OQ-20's folder behaviour stands for the lens grid | **no longer blocking**: built to those defaults, cheap to flip |
| OQ-74 | **Both of the tool's inputs now come from Ben, so there is nothing to scan before he is finished.** New 2026-09-21, out of correcting `PRD.md` section 4. Identity and encoding arrive in Ben's metadata CSV (`Shot`, `Shot Type`, `Gamma Notes`, `Color Space Notes`) and the conform and grade in Ben's EDL. The old flow had two phases on purpose: scan and review a turnover off the shooters' own timeline, then ingest Ben's colour later, with `PRD.md` section 6 steps 3 to 5 and the UI built to it - a turnover with no colour session is held back while the rest of the batch delivers, and the editor reviews rows before Ben has touched them. **That first phase no longer has any content**: without the CSV a row has no shot code, no clip type and no encoding, so it cannot be named, checked against the naming spec, or colour-resolved, and what the editor would be reviewing is a list of camera filenames. Options, none chosen: **(a)** accept it, scanning happens after Ben and the flow becomes one phase, which is simpler and drops the held-back-turnover behaviour; **(b)** the shooters' own metadata CSV goes in the turnover folder as it did in `Turnover199`, the scan reads whichever is present and Ben's supersedes it on ingest, which keeps both phases at the cost of two carriers for one fact; **(c)** the editor scans late by convention and nothing changes in code. The sample supports (b) being possible: its CSV sat in the turnover folder and came from the shooters' project  **ANSWERED 2026-09-21 by the user: (a), and it is by design.** "There is nothing to do or scan until Ben points the tool to the folder of media files including the edl and csv. Ben makes all the decisions." So the handover is **one folder holding the media, the EDL and the CSV together**, the flow is one phase, and a folder missing either file cannot be scanned at all rather than being scanned and held back. **This dissolves OQ-53** and retires the `_color` discovery convention built in M5.13, and it removes the per-turnover held-back behaviour, since a turnover with no colour session is now a folder the tool refuses to scan. The Ingest step survives only for a **revised** EDL | (a): one folder, one phase, scan reads the EDL and the CSV | no, answered |
| OQ-75 | **ALE instead of the CDL, and probably instead of the CSV too.** Raised by the user 2026-09-22 as a direction, not a build: "until we move to using ALE files rather than the CDL, the colour inputs will be found in these metadata slots". An Avid Log Exchange file is tab delimited, carries an arbitrary column set per clip, and can carry `ASC_SOP` and `ASC_SAT` alongside the identity and camera columns - which is to say it could carry **everything the tool currently reads from two separate files**, Ben's EDL and Ben's CSV, in one. Resolve exports ALE. What it would not carry is the approved In/Out, which is an edit rather than a clip fact, so an EDL or something like it probably survives beside it. Worth scoping before it is asked for: whether Resolve's ALE export includes `Gamma Notes` and `Color Space Notes`, whether it includes `Shot Type`, and whether its CDL columns come off node one like the EDL's do. Until then the carriers are as built: identity and encoding from the CSV, conform and grade from the EDL  **RAISED AGAIN 2026-09-22 by the user as a live alternative**: "ben will include an ALE file in the folder. This is more complete color info and edl." Scoped the same day against the Resolve manual, Avid's EDL Manager guide and Pomfort's format write-up, and **three of the four unknowns are answered**. **(1) The CDL travels.** Resolve exports it: Edit page, right-click the timeline in the Media Pool, **Timelines > Export > ALE and CDL**, which writes `ASC_SOP` and `ASC_SAT` columns per clip, the same node-one primaries the EDL's comment lines carry. **(2) Identity and encoding travel, on this version.** Resolve's ALE export writes a column for every Media Pool metadata field that has a value, with no setting to choose them, so `Shot`, `Shot Type`, `Gamma Notes` and `Color Space Notes` all appear once they are filled. **Custom** fields needed Resolve **20.3** (December 2025), which added defining and exporting them; the shooters run **21.1.0.0017**, so their custom `Shot Type` travels too. That means an ALE could replace the CSV outright. **(3) An ALE is a clip log, not a cut.** It carries **no record timecode** - Avid's own EDL Manager assigns record timecodes on import because the file has none - so it cannot state a cut order, and the single-rate header means one frame rate per file, which at 24 costs nothing. **(4) The one thing that decides it, still unverified: does Resolve write each row's `Start`/`End` as the timeline's trimmed range, or as the full source clip?** Trimmed, and the ALE carries the approved In/Out and **replaces both the EDL and the CSV**. Full source, and it carries no cut at all and **the EDL must survive beside it** for the conform while the ALE replaces only the CSV. This is one file to answer: one ALE and CDL exported off a trimmed timeline, compared against the EDL from the same timeline. Fold it into OQ-55's test export rather than asking twice. **ANSWERED 2026-09-22 by a real ALE**, which the user put in `Turnover199/`. `docs/SAMPLE_TURNOVER_199.md` section 8 is the measurement. **The ALE replaces the CSV and cannot replace the EDL.** It carries `Shot`, `Shot Type`, `Gamma Notes`, `Color Space Notes`, `ASC_SOP`, `ASC_SAT`, `Shot Code`, `RESOLVE_SIZING` and a `Source File Path` pointing at the delivered media, in 49 columns with **no duplicate column name**. **But it does not solve the problem the user wants it for** (2026-09-22): its only encoding columns are `Gamma Notes` and `Color Space Notes`, the same hand-typed Media Pool free text the CSV carries, and **`Input Color Space` is absent** - Ben's dropdown choice is a Resolve clip *property*, not a metadata field, which is why neither export carries it. What is machine-generated in the ALE is `ASC_SOP`, `ASC_SAT` and `RESOLVE_SIZING`, all grade rather than input transform. So an ALE carries what the shooter typed and **not** what Ben selected, and QC-047 stays exactly as load bearing as it was. **Its timecodes are also a hybrid and must not be conformed on**: `Start` is the original camera clip's head (identical to the CSV's `Start TC`) while the length is the consolidated file's, so `Start` sits 43 to 167 frames before the delivered file's own timecode, by a different amount per clip. But **`End - Start` is the whole delivered file on all five clips** - 280, 248, 49, 49, 49, matching ffprobe exactly - while the `.drt` beside it records a real trim on every one (`In` 24, `Duration` 232 / 216 / 1). **The export ignored a trim that existed in the same project**, and its rows are in the timeline's own record order, so it was a timeline export that knew the cut and declined to state it. An ALE carries no record timecode and, measured here, no source trim either. **The handover is therefore media + EDL (the cut) + ALE (identity, encoding, grade)**, and the CSV drops out. Two rules to settle before adopting it: the CDL appears in both the EDL and the ALE, so one has to win and the other has to be QC'd against it; and this ALE carries a CDL on the reference still rows, which are delivered ungraded, so the reader must ignore it there. A third finding came free: **the CSV in that folder is stale** - its `Clip Directory`, `Frames` and `End TC` describe the pre-consolidation originals - so nothing may read a duration or a path out of a metadata CSV. **CLOSED 2026-09-22: the ALE is not adopted** (user: "Let's skip the ALE"). The carriers stay exactly as built - **Ben's EDL for the cut and the grade, Ben's metadata CSV for identity and encoding**. The reasoning, in one line: the ALE could not carry the cut, did not fix the misspelling problem it was wanted for, duplicated the CDL the EDL already carries, and its timecodes were a trap; what it did offer was one failure mode the CSV reader can handle itself, a stated frame rate, and a reframe field that was identity on every clip. Not worth a second reader and a CDL reconciliation rule. **What would reopen it**: Ben reframing shots, since `RESOLVE_SIZING` is the only carrier for that and nothing else in the folder would show a lost reposition; or Ben preferring one export to two. **What survives the decision and is now load bearing**, because the CSV is confirmed as the carrier rather than merely the incumbent: the **duplicate `Shot Type` column** must be arbitrated deliberately (QC-065), and **no duration, frame count or path may be read out of the CSV** - measured stale against the delivered media in `SAMPLE_TURNOVER_199.md` section 8.| Deferred. Recorded so the scoping is not redone from scratch | no, deferred |
